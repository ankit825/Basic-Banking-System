import React from 'react';
import FacebookIcon from '@material-ui/icons/Facebook';

const Icon = () => {
  return (
    <div>
      <FacebookIcon />
    </div>
  );
};

export default Icon;
